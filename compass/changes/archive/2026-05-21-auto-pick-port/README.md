# auto-pick-port

If the default port is in use, scan upward and pick the first free one. Applies to both compass web and compass docker up when --port isn't explicitly passed; explicit --port keeps strict behavior.
