# docker-runner

compass docker up/down/logs/status subcommand that runs compass web in a container with the project's compass/ dir bind-mounted; web app refetches on tab focus so live edits show without manual refresh
